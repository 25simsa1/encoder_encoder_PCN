# State (updated 2026-07-23)

## Paper status (2026-07-23)
The writing pass is underway on docs/paper/PAPER_DRAFT_v2.md (headline 3-seed category-transfer table complete, see LOG 2026-07-23). The hero results figure is DONE: figs/fig2_results.{pdf,png} (matched fit / category transfer / per-category dumbbells) replaces fig2/fig3 in the draft, instance hits are a table. Remaining for submission: related work + citations, LaTeX/ICLR conversion (confirm column width vs the 6.75in strip; consolidate the duplicated instance-hits table; decide if Sec 5 needs a figure since fig3 retired), optional 8k-ckpt scale-trend probe.

NOVELTY HARDENING (planned 2026-07-23, awaiting go): the single-scale result reads as "BP beats local rules, known"; the plan at docs/superpowers/specs/2026-07-23-ladder-plus-fa-campaign.md (mock figure alongside) runs the capacity ladder 330M-7.7B under the stability recipe with category_probe per rung (seed-0 ~105-130 GPU-h) plus a feedback-alignment fork of E1L at 156M/20k as the locality-vs-recipe control (<10 h + ~1 day implementation). Ladder: no runs launched. FA CONTROL: fork IMPLEMENTED and validated 2026-07-23 -- experiments/run_FA_lars_infonce.py (E1L untouched), FA_MODE random|transpose, feedback via a native-op stop-gradient composition (forward bit-exact, dW true, dx through B). Both CPU smoke gates PASS: transpose reproduces E1L digit-for-digit, random trains/falls/no-NaN with a ckpt key-identical to E1L (see LOG 2026-07-23). Ready to run 156M/20k x3 seeds on go.

## BREAKTHROUGH (2026-07-19)
First per-image-structured generation. The recipe, COCO64_WIDE (inter_dim 512) + recon at lr 1e-3 unclipped iso 1e-3 (ckpt_wide5_best, the wide runs away in weight space at lr>=2e-3) + closed-form ridge on EVERY untied td edge, dense AND stride-1 conv (ridge_td.py --ridge-conv, no distillation stage; the NLMS conv distillation provably diverges on wide state statistics). All edges heal by construction (conv R2 0.996-0.9999, dense down-pipes 0.69-0.99, gains ~1), the decode is relaxation-sane, and the rms-boost decode from image-set latents hits mse 0.0420 at contrast 0.854, beating the 0.0523 benchmark and the 0.069 mean-predictor bar, with the grid showing per-image composition tracking each true image. Text-set decode is still the template (0.0801). Caption-to-image is now BLOCKED by the cross-modal coupling failure itself (see 2026-07-20 below), not by a readout gap.

## Caption-to-image verdict (2026-07-20)
Three alignment routes all null (closed-form text ridge, and textdistill local recon at lr 5e-3 / 1e-3+clip / 1e-3+clip+iso). The cross-modal ceiling is ~0, the image latent is only 0.002-0.18 R2 linearly recoverable from the text states per tap, and neither closed-form nor local distillation raises it (textdistill floor stalls at the seed 0.0317). The text trunk never encoded image-aligned per-caption identity at 156M / 2k overfit, so caption-to-image is blocked by THE coupling failure this project studies, not a readout gap. Image-to-image generation (mse 0.0420, per-image structure) stands as the banked win.

## Trunk-level coupling attempted and definitive (2026-07-21)
Ran direction 1 (make the text trunk couple). Fixed the InfoNCE no-op (scale-robust code nudge, image anchored), lambda-probed it, then trained recon+InfoNCE from wide5_best at batch 8 and a definitive hard batch 32 (40ep), each with a re-solved ridge decode + battery. Coupling accuracy reaches 1.0 (32-way, chance 0.031) and the image path stays healthy (0.0402), but caption-to-image does NOT move, text-set decode 0.0794 (template) and the cross-modal ceiling R2 is unchanged. InfoNCE buys IDENTITY/retrieval alignment, not VALUE alignment (the text code never becomes the decodable image latent). Four independent routes now agree (closed-form text ridge, textdistill x3, InfoNCE batch-8/32) that the image latent is ~0-19 percent linearly recoverable from text, so the cross-modal coupling failure is REPRESENTATIONAL at 156M / 2k, the project's central thesis, now shown with a mechanism. This is the STOP point for the single-rung 2k study. It feeds the capacity ladder directly (does the representational coupling limit persist 156M->7.7B). Deliverables, (1) image-to-image generation 0.0402 with per-image structure, (2) the localized, mechanistically-explained coupling-failure characterization. Next is a lead scope call, run the ladder, or write up these two results. Caveats, structure is luminance-layout level (not sharp objects, checkerboard artifact from the strided transpose), and the readout is the rms boost, not free relaxation. Details in LOG.md 2026-07-19.

## Where things stand (2026-07-17)
The generation campaign REOPENED after the lead amended the design rule, the image path's top-down direction now gets its OWN prediction weights (`--untied`, wts_td + c_td per edge, still one net, reciprocal edges, relaxation inference, local learning only, byte-identical off). Two root causes then fell out of the top-to-bottom audit. First, the FLATTEN BUG, `FlattenPCNLayer.pred_loss_d_input` returned a constant 1. instead of 0. since the original commit (2026-06-07), invisible at recon rates, catastrophic at decode rates; fixed, gate re-banked (gate_ref_coco64_v2), image-level content transport jumped 230x. Second, the copy-init AMPLIFIER EDGES (the five wide-relu denses predicting 100-dim inters, round-trip gain 30-73) defeat every first-order local rule (LARS, trust caps, NLMS at any stable rate); resolved by fast-forwarding the local rule to its provable fixed point with a closed-form dual ridge solve (tools/ridge_td.py), which healed ALL dense edges exactly (gains 0.99-1.08, cos 0.999).

THE RIDGE VERDICT (2026-07-17) then settled the remaining question. With mathematically optimal top-down edges, generation is STILL a shared attractor under both readouts (single-chain rms boost and multi-branch pi relaxation), so the per-edge-linear ceiling is PROVEN, no local training on this architecture can beat what was tested. The measured cause is the inter_dim=100 hourglass, the ridge R2 map shows every upward edge at 0.996-1.0000 while the five downward 100-dim pipes carry only 57-72 percent of the below-layer variance, and the relaxation resolves that irreducible top-down error by settling to the data mean.

CURRENT ESCALATION (in flight), COCO64_WIDE, inter_dim 100 -> 512 (2560 total downward rank, above the PCA-500 recognizable-blurry ceiling), a new gated config (coco64w), NATIVE and COCO64_GEN untouched. Chain on HEQ, 9147 (from-scratch recon 15ep --isometry 1e-3 -> ckpt_wide_best) -> 9150 (ridge -> ckpt_wide_best_td, conv td copy-init) -> 9151 (edge_gain + content_trace + boost/pi grids latent_wide_{boost,pi}.png). After the verdict, the queued follow-ups are text-to-code alignment (fix B) for caption-to-image and the distill-then-join run (recon --untied warm-started from healed weights) to retire the phased-training caveat.

Historical record of the closed shared-weight campaign below (kept for the paper's characterized negatives).

## Current hypothesis
The text-to-image failure has been decomposed down to one hard sub-problem. Fixed/understood so far: the text path is healthy and sets the shared latent at the right scale; invertible strided-conv downsampling reconnected the generative pathway the one-way maxpools had severed; a top-down-authoritative generation schedule makes the caption drive the image per-pixel (PR 0 -> ~6). What REMAINS is amplitude: the top-down decode produces a dark, low-contrast, low-frequency image (gen brightness ~0.4x the true image, never sharp). A darkness diagnostic showed this is NOT the latent (text-set latent scale matches image-set) and NOT weight decay (decode weight-norm unchanged) -- it is the top-down PC decode producing the low-amplitude CONDITIONAL MEAN (the blurry average image consistent with the latent). The generative-training objective (two-phase: text-drive the latents, then bridge to the true image clamped at the bottom, local weight step) trains stably at a gentle schedule but does NOT sharpen -- it actually darkens over steps, because the clamped true image "helps" during the weight step so the decode never learns a strong standalone latent->image map. The generative objective was then redesigned as a contrastive-Hebbian (CHL) pass that supervises the PURE top-down decode toward the true image, but it (and every prior generative objective) DESTABILIZES at ~ep13 the same way: NORM INFLATION (the beta-less LARS trust ratio grows with ||w||, a positive feedback that inflates weights and states until the state clip and the energy explode). So the objective is not the blocker; the norm-inflation instability is, and it caps how long any generative training can run.

CURRENT FIX (in flight): a PC-native weight-normalization stabilizer. Each opt-in conv/dense weight is reparameterized w = g_mag * v/||v|| (per-output-unit magnitude + unit direction, the SAME shared weight both directions so it stays bidirectional PC), and update_wts splits the class's own local gradient into a radial magnitude step + a tangential direction step so ||w|| cannot run away; LARS trust is dropped for these layers. Opt-in via --weight-norm, byte-identical when off. RESULT (2026-07-11): the weight-norm stabilizer SHIPPED and WORKS (conv c0a4d42, dense e0ba22e, --weight-norm 45274ec, trust-fix 7d8f50e; 7 unit tests; COCO64 gate byte-identical when off, GATE_MATCH nlayers=88; NATIVE-143 gate deferred to a big-GPU window). One correction, dropping the LARS trust exploded fresh-init in one step, so trust is KEPT but on the stabilized ||v|| (both weight-norm AND trust are needed). The warm-started CHL retrain (job 8845) now trains STABLY past ep13 (energy bounded 0.07 vs the prior 7-10 blowup), with a residual energy-bounded state drift toward the clip.

BUT the make-or-break retest REFUTES the "instability is the blocker" hypothesis: with stability fixed, the CHL objective does NOT sharpen text-to-image and FLATTENS it with training (boosted-gen contrast 0.404@ep1 -> 0.243@ep12; brightness stuck at the ~0.40 conditional-mean). So the true ceiling is the GENERATIVE OBJECTIVE (squared-error PC decode converges to the blurry conditional mean; more stable training makes it flatter), NOT the training stability. The stability fix was necessary to isolate this, and the weight-norm code is a keeper for any longer generative training.

Approach A (high-frequency-weighted energy) SHIPPED and was tested: a high-pass boost on conv1's bottom pixel error (opt-in --hf-weight, byte-identical off, GATE_MATCH nlayers=88, composes with weight-norm). VERDICT = NULL over a full gamma sweep (contrast flat 0.24->0.25 across gamma 0/1/2/3; brightness stuck ~0.40). A high-pass confined to the last pixel-mapping cannot reach the blur, which is set upstream by the whole decode chain + the shared latent. This reinforces the mean-collapse diagnosis (a deterministic error-reweighting cannot beat the conditional mean). The HF code stays in the tree (opt-in, inert off) as a characterized negative.

Approach B1 (PC-native EBM by noisy relaxation) SHIPPED and was tested: opt-in Langevin noise in update_state (byte-identical off, GATE_MATCH nlayers=88) + a contrastive-divergence ebm_step (chl_step with a noisy CD-1-from-data negative phase). VERDICT = NULL. Training is stable, but generation fails: the deterministic mode is degenerate (0.28/0.13, worse than plain CHL) and SAMPLING gives pure RGB noise (samples mean ~0, std 0.5-1.3, values far outside [0,1]). The CD-1 contrast did not carve sharp energy wells, so there is no Langevin sweet spot (small noise -> blurry mode, large noise -> static). The B1 code stays in the tree (opt-in, inert off) as a characterized negative.

So the mean-collapse has now survived a deterministic error reweighting (Approach A) AND a Langevin EBM (B1). Two clean nulls. The remaining principled sampling option is the DIFFUSION flavor (B3), which explicitly learns a multi-noise-level denoiser (noise->data at each level) and is the proven cure for blurry generation, but it is the biggest lift and furthest from the current machinery.

Approach B3 (diffusion-in-PC) SHIPPED and was tested (diffusion_step over a 10-level schedule, byte-identical off, no layer change). VERDICT = NULL. Training is stable, but the direct denoiser test is conclusive: the "denoised" MSE-to-true EQUALS the noised MSE exactly at every level, so x0_hat = x_t -- the net trained into a PLAIN AUTOENCODER (reconstruct the input) and never denoises. Root cause: bidirectional PC's SHARED WEIGHTS default to reconstructing the input, reinforced by the clean-recon interleave, so the x_0 target never shifts the map off identity. Reverse diffusion from noise therefore stays noise. B3 code stays in the tree (opt-in, inert off).

THREE clean nulls now on the mean-collapse, each with a clear mechanistic reason: A (deterministic HF, can't beat the conditional mean), B1 (Langevin EBM, energy never sharpened so samples are noise), B3 (diffusion, shared weights collapse the denoiser to identity). Sharp from-scratch 64px text-to-image is proving very hard for this bidirectional-PC design, and each null pins down why.

The latent-source probe (2026-07-12) then REFRAMED the whole failure. Decoding from IMAGE-set latents (correct content at every scale) still yields only a brightness-modulated luminance template, and per-scale swaps show 4 of 5 latents are completely inert under the boosted route (only the deepest code matters, and it only sets brightness). So the binding constraint is NOT the latent content and NOT the fine-latent attenuation. The top-down decode was never trained to be top-down self-sufficient, recon always clamped the image (intermediates bottom-up), and every generative objective so far conditioned on TEXT-set latents (the ill-posed conditional-mean setup).

The latent-autoencoder CHL (fix A, --gen-latents image, job 8936) then RAN AND FAILED, and the failure completes the characterization. Even with the well-posed target (image-set latents, whose conditional mean IS the true image), the decode converged to the same low-amplitude template, now dimmer than untrained (0.53/0.26 vs 1.09/1.31). And the gamma=0 readout is the smoking gun, plain relaxation decodes EVERY latent source to exactly zero. So the root is STRUCTURAL, the top-down pathway of this shared-weight net is CONTRACTIVE (latent signal dies across the 9-layer decode without a boost) and the only revival route (the gamma=1 replacement boost) is single-chain feedforward that destroys all but the deepest code's information. Local contrastive training cannot fix it because the free phase itself collapses to zero, a degenerate negative sample.

THE CONTRACTION SWING (user-directed, the last generation lever) RAN AND IS EXHAUSTED (2026-07-13). The precision probe first corrected the record, the earlier zero-decode was RATE STARVATION (the diagnostics build at state_lr 1e-4, so relaxation from zeros barely moves) and at an adequate rate the top-down cascade EXPRESSES but is miscalibrated, a content-blind saturated output at every schedule (pi_bu 0/0.3/1.0, means 3.4-8.6, latent source irrelevant to the 3rd decimal). Then the training half, the calibration CHL with an expressing free phase, destabilized at BOTH expression levels (full = recon weights wrecked in 150 steps, gentle = accelerating degradation pinned at the clip by ep2), while the degenerate expression level (the original latent-AE CHL) merely flattens. The window between a degenerate negative and a destabilizing negative is EMPTY.

THE GENERATION QUESTION IS CLOSED AS A CHARACTERIZED NEGATIVE (pending the lead's sign-off). Campaign tally, seven generative objectives (gen, gentle-lr, CHL, HF-weighted, Langevin EBM, diffusion-in-PC, latent-AE/calibration CHL), two inference routes (the replacement boost and the precision schedules across the rate sweep), every failure with a pinned mechanism, and every mechanism's code opt-in and byte-identical off (gated). The shared-weight bidirectional-PC design at 64px cannot render latent identity top-down and cannot be trained to by any local contrast tried, and the full why is documented in the LOG.

THE FINAL SWING (the isometry constraint, user-directed) RAN AND NULLED (2026-07-13). The scaled semi-orthogonalization trains FREE (same 0.006 recon floor, lower bounded states, a genuine stabilizer) and fixes the gain pathology, but generation is still content-blind, the pure cascade saturates and the boost now over-drives to a blown-out field, with the text-set decode a brighter template (0.64/0.62, the campaign's best numbers, still no identity). The residual killers are structural and transpose-independent, GELU and the strided downsampling are not inverted by any transpose.

THE GENERATION CAMPAIGN IS CLOSED. Eight mechanisms (gen, gentle-lr, CHL, HF-weighted, Langevin EBM, diffusion-in-PC, latent-AE/calibration CHL, cascade-consistency, the isometry constraint) and three inference routes (boost, precision schedules, rate sweep), every failure with a pinned mechanism, all code opt-in and inert off. Transferable wins for the paper's core, weight-norm and the isometry constraint are free stabilizers (directly relevant to the ladder's norm-inflation failure mode), plus the corrected monitoring and the HEQ scheduling route.

NEXT, bank the chapter and run the coupling ladder (156M -> 7.7B at 8k vs the >3-in-2000 bar). The big rungs need the whole H200 (frees when the user's other-session p3-control ends); the small rungs and all tooling fit the HEQ slices now.

Separately, the broader banked question (does the coupling failure persist 156M -> 7.7B, judged at 8k against the bar) still stands via the capacity ladder.

## In flight
- The COCO64_WIDE chain (jobs 9147 -> 9150 -> 9151 on HEQ), wide-inter encoder training, ridge solve, generation battery. The resume map lives in `.superpowers/sdd/progress.md`.

## Next steps
- Read the wide battery verdict (R2 map, edge gains, latent_wide_boost/pi grids) and judge per-image identity.
- If identity survives, wire text-to-code alignment (fix B) for caption-to-image, then the distill-then-join run.
- Deferred, NATIVE-143 gate re-bank post-flatten-fix (needs the whole H200), then the capacity-ladder training runs per docs/runbooks/CAPACITY.md.

## Open questions
- Which pre-registered branch the capacity ladder lands on. PC stays flat at chance through 7.7B while the baseline crosses at every size, PC crosses the bar at some size, or the baseline degrades at large capacity.

## How to update this file
Overwrite the fields above as work moves. Append the detailed run records to `docs/experiments/LOG.md` instead of here.
